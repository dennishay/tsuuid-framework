__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/

# Models live in Dropbox (__768_sync/tsuuid-framework-models) and are
# symlinked into this tree. Large binaries exceed GitHub's 100 MB cap, so
# the entire models/ path stays out of the repo — including the symlink.
models
models/
